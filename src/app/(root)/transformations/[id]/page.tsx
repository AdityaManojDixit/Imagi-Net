import React from 'react'

//Each transformations is unique so it wont happen on /tarnsformations
//Each transformation will have its own id
const TransformationsPage = () => {
  return (
    <div>TransformationsPage</div>
  )
}

export default TransformationsPage